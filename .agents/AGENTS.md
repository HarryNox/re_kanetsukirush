# Versioning Rule
Every time you modify the game's code, you MUST:
1. Increment the version number in `index.html` inside the `<span class="version">verX.X</span>` tag.
2. Commit and push the changes to GitHub with a commit message starting with the new version (e.g., "v1.1: Fix bug").
